#ifndef LEVEL_H
#define LEVEL_H

#include <vector>

#include "Entity.hpp"
#include "Bullet.hpp"

class Level
{
	public:

		Level();
		~Level();
		void add(Entity* e);
		void add(Bullet* b);

	private:

		std::vector<Entity*> m_entities;
		std::vector<Bullet*> m_bullets;

};

#endif
